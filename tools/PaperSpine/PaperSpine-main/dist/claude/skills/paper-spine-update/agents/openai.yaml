interface:
  display_name: "PaperSpine Update"
  short_description: "Check and update PaperSpine"
  brand_color: "#2563EB"
  default_prompt: "Use $paper-spine-update to check and update PaperSpine while preserving global settings."

policy:
  allow_implicit_invocation: true
